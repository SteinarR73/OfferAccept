# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/

# Environment files — never commit any .env variant
.env
.env.*
.env.local
.env.*.local

# Turbo
.turbo/

# Prisma
# Keep migrations in source control, but not the generated client
packages/database/node_modules/

# SQLite / database files — never commit; dev/test artefacts only.
# If a .db file is already tracked: git rm --cached <path>
prisma/*.db
prisma/*.db-wal
prisma/*.db-shm
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Security audit outputs — contain redacted credential info, never commit
docs/security/audit-report.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage
coverage/

# Type declarations (generated)
*.d.ts.map

# TypeScript incremental build cache — never commit build artefacts
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
